<view class="swiper-three-dimensional" bindtouchstart="touchstart" bindtouchend="touchend">
	<view wx:if="{{index>=currentIndex-can_show && index<=currentIndex+can_show}}" class="swiper-item {{swiperClass[index]}}" wx:for="{{listData}}" wx:key="listData">{{index}}</view>
</view>